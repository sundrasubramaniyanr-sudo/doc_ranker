# Donation & Basic-Needs Management System

Streamlit prototype for the Community Connect project.

Run:
`pip install -r requirements.txt`
`streamlit run app.py`

SQLite database is created automatically.
